// In frontend or worker
import { User } from '@shared/user.dto';

const user: User = {
    id: '1',
    name: 'John Doe',
    email: 'john@example.com',
    password: 'password'
};

console.log(user);
